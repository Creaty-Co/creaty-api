on:
  push:
    branches:
      - task-275

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - uses: ./.github/workflows/call-ci.yml@task-275
  deploy:
    runs-on: ubuntu-22.04
    environment:
      name: dev
    steps:
      - run: echo ${{ vars.TEST_VAR }}
